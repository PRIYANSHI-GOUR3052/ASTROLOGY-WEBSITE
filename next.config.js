/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'randomuser.me',
      'res.cloudinary.com',
    ],
  },
}

module.exports = nextConfig 